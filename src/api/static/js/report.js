// 占位：Task 10 实现完整报告视图（本文件将在 Task 10 被替换）
export async function openReport(symbol) {
  console.warn("报告视图尚未实现", symbol);
}

export function initReportView() {
  document.getElementById("stockBtn").addEventListener("click", () => {
    console.warn("报告视图尚未实现");
  });
}
